# Android 真机/ADB 评估模块清单

模块：`adb_manager` / `apk_analyzer` / `vulnerability_scanner` / `exploit_engine` / `network_scanner` / `payload_generator` / `report_generator`。

与现有 `apk-reverse` / `apk-redteam-pipeline` / `android-app-pentesting` 配合：
- 静态/逆向走 apk-reverse
- 真机 ADB、设备管理、动态利用面走本目录

产品壳不作为独立 Hermes 路由技能；按攻击面引用。

---
@pyufz · @TGSEC-yufeifei 整理
