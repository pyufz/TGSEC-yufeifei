# recon-skills 融合说明

按攻击面拆入各域 `src-methods/`（及少数 redteam/auth/cloud）。
新增约 101 个原先缺失的 skill 目录；与 Claude-BugHunter / 已有 hunt-* 重名的只在更丰富时写入 `references/upstream-recon-skills*`。
优先增量字段经验：write-gap / schema-enum / metrics / origin-ip / CORS 变体。

---
@pyufz · @TGSEC-yufeifei 整理
